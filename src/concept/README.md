# Concept Lane

This folder contains the active UI / UX concept prototype.

Current file:

- `InspectionSetupPrototype.tsx`

Use this lane for:

- customer walkthroughs
- flow validation
- layout / interaction exploration
- concept-level export and review ideas

Do not use this lane as the real product implementation target.

Real product build lanes live in:

- `apps/field-android/`
- `apps/report-platform/`
- `packages/canonical-schema/`
